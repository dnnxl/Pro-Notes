
/*
class BriansBrain extends Automaton
{
  BriansBrain(float w)
  {
    super(w);
  }

  Cell createCell(float x, float y, float w)
  {
    return new BriansBrain(x, y, w (int) random(3));
  }

  void next()
  {
    for (int r = 0; r < rows; r++)
    {
      for (int c =0; c < cols; c++)
      {
        int n = aliveNeighbors(r, c);
        Cell c = cells[r][c];
        if(c.state = 0 && n ==2) c.newState = 2;
        if(c.state == 2) c.newState = 1;
        if(c.state == )
      }
    }
  }
  
  int aliveNeighbors(int r, int c)
  {
    
  }
  
  void click
  
}
*/
